pre-commit:
  parallel: true
  commands:
    format:
      glob: '*.{js,ts,svelte,json,css,md}'
      run: bun run format {staged_files}
    lint:
      glob: '*.{js,ts,svelte}'
      run: bun run lint --fix {staged_files} || true
